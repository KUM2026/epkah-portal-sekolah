// Contoh: Update statistik jika ada API
document.addEventListener("DOMContentLoaded", () => {
    const stats = document.querySelectorAll(".footer-stats li span");

    // Contoh: fetch data dari API (dummy)
    // fetch('/api/visitor-stats')
    //   .then(res => res.json())
    //   .then(data => {
    //       stats[0].textContent = data.online;
    //       stats[1].textContent = data.today;
    //       stats[2].textContent = data.yesterday;
    //       stats[3].textContent = data.total;
    //   });
});
